export interface IPomSignature {
  reportAuthor: string;
  prison: string;
  counterSignature: string;
  counterSignatureRole: string;
  startDate: string;
  reportDate: string;
  saved: boolean;
  valid: boolean;
}
